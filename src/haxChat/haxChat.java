package haxChat;

import ui.startUp;
public class haxChat {
	public static void main(String[] args) {
		
		//Not working setup
		startUp init = new startUp();
		//Working setup
		/*
		 * UI g = new UI();
		 */
	}

}
